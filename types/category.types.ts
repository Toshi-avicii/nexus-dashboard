export type NewCategory = {
    name: string;
    description: string;
}

export type FetchedCategory = {
    createdAt: string;
    description: string;
    name: string;
    updatedAt: string;
    _id: string;
}